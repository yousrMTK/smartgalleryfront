import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartgallery',
  appName: 'Smart Gallery',
  webDir: 'dist/smartgalleryfront',
  bundledWebRuntime: false
};

export default config;
